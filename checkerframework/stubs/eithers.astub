import org.checkerframework.checker.nullness.qual.*;

package de.redsix.dmncheck.util;

public static class Eithers$PartialMatcher<A, B, X> {
      private final Either<A, B> _either;

      @Nullable
      private final Function<A, X> left;

      @Nullable
      private final Function<B, X> right;

      PartialMatcher(Either<A, B> _either, @Nullable Function<A, X> left, @Nullable Function<B, X> right);
}
